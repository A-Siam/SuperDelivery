# Contribution rules for Ordering Service
- each file contain a single concern
- all similar entities are grouped under a single package
- all file names are in kebab-case _i.e._ `order_controller.go`

